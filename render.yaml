services:
  - type: web
    name: backend
    runtime: node
    buildCommand: npm run build
    startCommand: cd backend && npm start
    env: production
    envVars:
      - key: PORT
        value: "5000"

