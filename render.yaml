services:
  - type: web
    name: backend
    runtime: node
    buildCommand: cd backend && npm install --legacy-peer-deps
    startCommand: cd backend && npm start
    env: production
    envVars:
      - key: PORT
        value: "5000"
